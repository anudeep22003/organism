commit_details.txt

*anudeep*
*.tasks*

# Agent notes — local only, never committed
.notes/

# Ignore local env overrides (contain real credentials) but allow
# committed env files like .env.production and .env.development
.env.local
.env.*.local
backend/.env.local
backend/.env.smoketest
